image_speed = 0;
image_index = irandom(4);
direction = random(360);
image_angle = random(360);
speed = 1 + random(3);
num = random(2)
if (num > 1.0) {
	image_xscale = num;
	image_yscale = num;
}